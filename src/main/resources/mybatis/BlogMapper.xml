<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tobby.dailyapp.blog.mapper.BlogMapper">
    <insert id="insertBlogFile">
        INSERT INTO daily_db.blog_s3_file (name, category)
        VALUES
        <foreach collection="names" item="name" separator=",">
            (#{name} , #{category})
        </foreach>
    </insert>
    <select id="getZipFile">
        SELECT id, name, uploaded AS done, UNIX_TIMESTAMP(created_at) * 1000 as createdAt, category
        FROM daily_db.blog_s3_file
        WHERE uploaded = 0
        ORDER BY id ASC
        LIMIT #{limit}
    </select>
    <update id="updateDoneFile">
        UPDATE daily_db.blog_s3_file
        SET uploaded = #{uploaded}
        WHERE id = #{id}
    </update>
    <select id="getZipFileByCursor">
        SELECT id, name, uploaded AS done, UNIX_TIMESTAMP(created_at) * 1000 as createdAt, category
        FROM daily_db.blog_s3_file
        <if test="cursorId != null">
            WHERE
            id &lt; #{cursorId}
        </if>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>
    <select id="getZipFileByOffset">
        SELECT  id, name, uploaded AS done, UNIX_TIMESTAMP(created_at) * 1000 as createdAt, category
        FROM daily_db.blog_s3_file
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
    <select id="totalZipFileCount">
        SELECT COUNT(id)
        FROM daily_db.blog_s3_file
    </select>
</mapper>
